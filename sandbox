#!/usr/bin/env sh
set -eu

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"

# Root-level entrypoint for macOS, Linux, and WSL. The real logic lives in
# scripts/sandbox.sh so Makefile and direct shell usage share the same path.
exec "$SCRIPT_DIR/scripts/sandbox.sh" "$@"
